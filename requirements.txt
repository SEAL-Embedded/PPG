# SEAL PPG — Python dependencies
# Install:  pip install -r requirements.txt

# Dashboard / web app
fastapi
uvicorn
pydantic
httpx                # required by fastapi.testclient.TestClient

# Serial receiver
pyserial

# Numerics / analysis pipeline
numpy
pandas
scipy
pingouin             # ICC (sqi/ICC.py)

# Plotting (signal_visualization/, sqi/bland_altman.py)
matplotlib

# Tests
pytest

# --- NOT required: pyhrv ---
# `pyhrv` pulls in `spectrum`, which needs a C/Fortran toolchain and often
# fails to build; it is also unmaintained and predates numpy 2. The dashboard
# does not need it: the frequency-domain HRV metrics (VLF/LF/HF power, LF/HF
# ratio) run on scipy.signal.welch in webapp/analysis.py:_freq_domain_metrics,
# using the same 4 Hz cubic-spline + Task Force 1996 bands method pyhrv used.
